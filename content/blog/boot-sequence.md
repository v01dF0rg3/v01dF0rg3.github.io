# boot-sequence

`status: online`

A blank prompt felt like the right place to start.

This site treats pages as directories and writing as files. The result is a
portfolio that can be explored instead of merely scrolled.

## Useful commands

- `help` shows the command index
- `ls` inspects the current directory
- `cd blog` moves through the site
- `cat boot-sequence.md` reads this post

More entries will land here as they are written.
