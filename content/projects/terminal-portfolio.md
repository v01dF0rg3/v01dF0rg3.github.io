# terminal-portfolio

`status: active`

A static portfolio that turns ordinary web navigation into a small zsh-like
filesystem.

## Built around

- an allowlisted command parser
- virtual paths and files
- keyboard history and tab completion
- responsive terminal output
- zero personal data collection

The terminal is a visual interface only. Commands never reach a real shell.
